{
  "ext": "ts",
  "ignore": ["*.test.ts"],
  "watch": ["src", ".env"],
  "verbose": true,
  "execMap": {
    "ts": "node -r ts-node/register -r dotenv/config --inspect"
  },
  "env": {
    "NODE_ENV": "development"
  }
}
